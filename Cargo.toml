[package]
name = "solo"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
rustyline = "18.0.1" # Great for input reading, history, and arrows support
ratatui = "0.30.2" # Or latest version
crossterm = "0.29.0"